# studio

Small art projects, experiments, TouchDesigner assets, and supporting code.

## Structure

- `projects/` - project folders and shared TouchDesigner tox components.
- `projects/toxes/` - reusable TouchDesigner components.
- project-local `data/` and `renders/` folders - checked-in inputs, generated data, and previews.
